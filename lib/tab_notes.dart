import 'package:flutter/material.dart';

class TabNotes extends StatelessWidget {
    const TabNotes({super.key});

    @override
    Widget build(BuildContext context) {
        return const Center(child: Text("Notes tab"));
    }
}