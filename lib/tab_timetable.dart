import 'package:flutter/material.dart';

class TabTimetable extends StatelessWidget {
    const TabTimetable({super.key});

    @override
    Widget build(BuildContext context) {
        return const Center(child: Text("Timetable tab"));
    }
}