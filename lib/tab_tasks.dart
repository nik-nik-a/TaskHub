import 'package:flutter/material.dart';

class TabTasks extends StatelessWidget {
    const TabTasks({super.key});

    @override
    Widget build(BuildContext context) {
        return const Center(child: Text("Tasks tab"));
    }
}